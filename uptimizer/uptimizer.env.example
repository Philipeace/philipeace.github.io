# Database Configuration
DB_HOST=uptimizer_db
DB_PORT=5432
DB_NAME=uptimizer_data
DB_USER=uptimizer_user
DB_PASSWORD=supersecretpassword

# Application Configuration
# !! REQUIRED !! Set a unique, long, random string here for security!
# Used for signing API tokens. Keep this secret! Generate one using e.g.,
# python -c 'import secrets; print(secrets.token_hex(32))'
SECRET_KEY=replace_this_with_a_very_strong_random_secret_key_32_bytes_or_more

# Optional: Set base path if running behind reverse proxy under subpath
# Example: APP_BASE_PATH=/uptimizer
# APP_BASE_PATH=/

# Optional: Override config path
# UPTIMER_CONFIG_PATH=/path/to/your/config.json